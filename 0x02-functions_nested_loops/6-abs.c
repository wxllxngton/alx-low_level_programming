#include "main.h"

/**
 * _abs - Check Description
 * @i: integer
 * Description: Gives the absolute value of a given integer
 *
 * Return: absolute value of number i
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (i * -1);
}
