#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 1 (Success)
 */

int main(void)
{
	char myStr[] = "\"Programming is like building a multilingual puzzle";

	puts(myStr);
	return (0);
}
